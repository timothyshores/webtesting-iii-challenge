import {
    render,
    fireEvent,
    cleanup,
    waitForElement,
} from 'react-testing-library'

afterEach(cleanup)

describe('<Dashboard />', () => {
    it('defaults to unlocked and open', () => {

    });
    it('shows the controls and display', () => {

    });
});